{
  "name": "impulso-local",
  "version": "1.0.0",
  "description": "Sistema de información del proyecto Impulso Local",
  "scripts": {
    "dev": "concurrently \"npm run dev:back\" \"npm run dev:front\"",
    "dev:back": "cd impulso_local_back && npm run dev",
    "dev:front": "cd impulso-local-front && npm run dev",
    "install:all": "npm install && cd impulso_local_back && npm install && cd ../impulso-local-front && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
